
import { SpinBox, Button, CheckBox, Slider, LineEdit, ScrollView, ListView,
    HorizontalBox, VerticalBox, GridBox, StandardButton, StandardTableView } from "std-widgets.slint";

export struct ViewToDoItem  {
    checked: bool,
    title: string,
}